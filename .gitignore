# We ignore the bin folder because it's generated by 'make build'
# This ensures platform-specific binaries don't clutter the repo.
bin/

# Crucial: prevents your local API keys/secrets from being leaked.
.rvenv_vars
*.vault
.env
.env.*

# Ignore the local identity config if it's created during development.
.config/
user.json

# Shell Junk
# Temporary files created by subshells or history
.bash_history
.rvenv_history

# System Junk
# macOS
.DS_Store
# Linux
.nfs*
*~
*.swp

# Linting & Logs
.shellcheckrc
*.log